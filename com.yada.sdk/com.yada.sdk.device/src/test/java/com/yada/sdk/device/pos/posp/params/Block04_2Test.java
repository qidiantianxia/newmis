package com.yada.sdk.device.pos.posp.params;

import org.junit.Before;
import org.junit.Test;

public class Block04_2Test {

    private Block04_2 block;

    @Before
    public void before() {
        String param04 = "";
        // TODO 获得国密算法银联新公钥参数的报文后再补全测试
//        block = new Block04_2(HexUtil.parseHex(param04));
    }

    @Test
    public void test() {
//        Assert.assertEquals("", HexUtil.toHexString(block.serviceId));
//        Assert.assertEquals("", HexUtil.toHexString(block.rid));
//        Assert.assertEquals((byte) 0x00, block.publicKeyStoreIndex);
//        Assert.assertEquals((byte) 0x00, block.publicKeyIndex);
//        Assert.assertEquals("", block.expiryDate);
//        Assert.assertEquals((byte) 0x00, block.hashAlgorithmIndicator);
//        Assert.assertEquals((byte) 0x00, block.publicKeyAlgorithmIndicator);
//        Assert.assertEquals("", HexUtil.toHexString(block.publicKeyModulus));
//        Assert.assertEquals("", HexUtil.toHexString(block.publicKeyHash));
//        Assert.assertEquals("", block.paramVersion);
    }

}